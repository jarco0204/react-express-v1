{
  "name": "react_express_app",
  "version": "1.0.0",
  "description": "public for static files server for express server     a)controllers for API controllers, b)routes for API endpoints     c)data for mock data src for React app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "start": "node index.js",
    "dev": "concurrently \"npm run client\" \"npm run start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
